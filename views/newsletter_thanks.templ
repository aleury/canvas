package views

templ NewsletterThanksPage(path string) {
    @Page("Thanks for signing up!", path) {
        <h1>Thanks for signing up!</h1>
        <p>Now check your inbox (or spam folder) for a confirmation link. 😊</p>
    }
}